# Vendored from https://github.com/mavroprovato/fuelpricesgr (MIT licence,
# master @ 2026-07-09) — the community parser for the Greek ministry's
# official fuel-price PDF bulletins (#3549). Only `parser.py` + `enums.py`
# are vendored (the upstream package also ships an API server, storage and
# mail plumbing we do not need). Upstream copyright: mavroprovato.
